@model Dashboard

<!--Se tendria que poder mandar una lista desde aca 
    Una lisya que contenga los dos dias , de este modeo al otro lado 
    solo seria necesario agregar esos dias en el dashboard-->

@using (Html.BeginForm("RegistraUsuarioTareas", "Login", FormMethod.Post))
{
    <div class="form-group mb-3 ">
        @Html.DropDownListFor(m => m.DiaSemana, ViewBag.DiaSemana as IEnumerable<SelectListItem>, new { @class = "form-control" })

    </div>
    @*<div class="form-group">
        <button type="submit" class="form-control btn btn-primary submit px-3">Registrar Tarea</button>
    </div>*@
}
